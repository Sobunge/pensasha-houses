{
  "properties": [
    {
      "name": "jwt.secret",
      "type": "java.lang.String",
      "description": "Secret key used to sign and validate JSON Web Tokens (JWT). Keep this value secure and do not expose it publicly."
    },
    {
      "name": "file.upload-dir",
      "type": "java.lang.String",
      "description": "Directory path where uploaded files are stored on the server. Can be absolute or relative to the application root."
    }
  ]
}