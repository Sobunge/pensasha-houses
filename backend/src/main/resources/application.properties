spring.application.name=backend

# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/pensasha_houses?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=sobunge
spring.datasource.password=samuel1995

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update

# Fix: Explicitly define the JDBC driver
spring.jpa.properties.hibernate.connection.driver_class=com.mysql.cj.jdbc.Driver

# Fix: Avoid unnecessary dialect warning (Hibernate auto-detects MySQL)
# Remove: spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Optional: Silence JTA platform warning
spring.jpa.properties.hibernate.transaction.coordinator_class=jdbc

